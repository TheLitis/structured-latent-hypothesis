# Shared-Latent Score Stability v1

## Result

- `shared_train` semi-real `S_joint` sign accuracy: `0.60+/-0.49`.
- `synthetic_only_train` semi-real `S_joint` sign accuracy: `1.00+/-0.00`.
- `shared_train` pooled `S_joint` sign accuracy: `0.82+/-0.22`.
- `synthetic_only_train` pooled `S_joint` sign accuracy: `0.98+/-0.04`.

## Interpretation

Synthetic-only training did not underperform on semi-real, so the current transfer claim is weaker than expected.
