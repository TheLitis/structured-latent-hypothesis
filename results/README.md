# Results Directory

Tracked outputs in this directory should stay lightweight and human-readable:

- summary JSON files,
- markdown reports,
- compact tables used in commit history.

Do not commit large raw artifacts here. Put heavy intermediates under `artifacts/`, which is ignored.
